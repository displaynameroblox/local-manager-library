-- well well well, a rewrite of the whole fucking library. that will surly will go well right???

local manager = {}

local _debug

function _debugtoggle(_Debug)
    if _Debug == true then
        _debug = true
    elseif _Debug == false then
        _debug = false
    else
        return "invaild act"
    end
end

function Media(islocal, isroblox, type, media)

end

return manager
